// later to be possibly replaced, loading from some EP
export const initialState = {};

const quizCreatorReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default quizCreatorReducer;
